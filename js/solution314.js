#include <stddef.h>

int grow(size_t size, const int arr[size]) {
  int prod = 1;
  for (int i = 1; i <= size; i++) {
    prod = prod * arr[i - 1];
  }
  return prod; //Your code comes here
#include <stddef.h>

int grow(size_t size, const int arr[size]) {
  int prod = 1;
  for (int i = 1; i <= size; i++) {
    prod = prod * arr[i - 1];
  }
  return prod; //Your code comes here
}
~
~
~
}
